# ask user for a string
input_str = input ("Enter a string:")

# print the characters placed on the even indices
result = [input_str[n] for n in range (0, len (input_str), 2)]

# print the result
print ("Characters at even indices:",','.join(result))